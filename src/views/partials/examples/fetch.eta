<div class='package-code'>
    <pre><code class='language-typescript'>const api = new FetchEngine({
    baseUrl: 'https://api.example.com',
    retryConfig: {
        maxAttempts: 3,
        retryableStatusCodes: [429, 502, 503]
    }
});</code></pre>
    <button class='copy-button' copy copy-text='const api = new FetchEngine({
    baseUrl: "https://api.example.com",
    retryConfig: {
        maxAttempts: 3,
        retryableStatusCodes: [429, 502, 503]
    }
});'>Copy</button>
</div>